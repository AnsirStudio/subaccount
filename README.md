# SubAccount

本地优先订阅管理 App。当前主路线是 Tauri + React + Tailwind CSS + shadcn/ui 风格组件。

## 开发

```bash
npm install
npm run dev
```

## 构建验证

```bash
npm run build
```

## Tauri 桌面版

当前项目已包含 `src-tauri/` 配置，可运行：

```bash
npm run tauri dev
```
